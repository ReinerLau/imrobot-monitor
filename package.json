{
  "name": "imrobot-monitor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "schema:build": "pnpm -F @imrobot/schema build",
    "db:init": "pnpm schema:build && pnpm -F @imrobot/db run init",
    "dev:test": "pnpm -F test-example dev",
    "dev:client": "pnpm -F client dev",
    "dev:serve": "pnpm -F api run start:dev",
    "commit": "cz",
    "prepare": "husky",
    "changeset": "changeset",
    "docker:start": "docker-compose up -d"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@commitlint/cli": "^19.0.3",
    "@commitlint/config-conventional": "^19.0.3",
    "commitizen": "^4.3.0",
    "cz-conventional-changelog": "^3.3.0",
    "dotenv": "^16.4.5",
    "husky": "^9.0.11",
    "tsup": "^8.0.2",
    "typescript": "~5.3.0"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
